# ─────────────────────────────────────────────────────────────────────────────
# GitHub Analytics Pipeline — Python Dependencies
# ─────────────────────────────────────────────────────────────────────────────

# ── Kafka ─────────────────────────────────────────────────────────────────────
kafka-python==2.0.2

# ── Spark ─────────────────────────────────────────────────────────────────────
pyspark==3.5.1

# ── Data Processing ───────────────────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
pyarrow==15.0.2
# fastparquet removed — not used anywhere; pyarrow is the sole parquet engine

# ── Snowflake ─────────────────────────────────────────────────────────────────
snowflake-connector-python==3.10.1
snowflake-sqlalchemy==1.6.1

# ── dbt ───────────────────────────────────────────────────────────────────────
dbt-core==1.8.4
dbt-snowflake==1.8.4
dbt-duckdb==1.8.1        # local dev target (no Snowflake needed)

# ── DuckDB (local dev) ────────────────────────────────────────────────────────
duckdb==1.0.0

# ── Power BI Export (CSV export script for Power BI Desktop) ──────────────────
# Power BI Desktop connects directly to Snowflake or consumes CSV exports.
# No extra Python packages required beyond pandas (already listed above).

# ── Airflow (via Docker — listed for reference) ───────────────────────────────
# apache-airflow==2.9.1

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
loguru==0.7.2
tenacity==8.3.0
requests==2.31.0
tqdm==4.66.4
